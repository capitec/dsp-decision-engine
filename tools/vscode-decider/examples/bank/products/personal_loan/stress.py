"""Personal loan: unsecured, 6 to 84 months. Stress tests: the instalment under rate shocks, and whether it still fits.

Generated by make_bank.py; edit the generator, not this file."""
from decider import flow, param, step


def pl_shock_25bp_instalment(requested_amount: float, requested_term: float, pl_rate: float, shock: float = param(0.0025)) -> float:
    r = (pl_rate + shock) / 12
    return requested_amount * r / (1 - (1 + r) ** -requested_term)


def pl_shock_25bp_fits(pl_shock_25bp_instalment: float, pl_affordable_instalment: float) -> bool:
    return pl_shock_25bp_instalment <= pl_affordable_instalment


def pl_shock_50bp_instalment(requested_amount: float, requested_term: float, pl_rate: float, shock: float = param(0.005)) -> float:
    r = (pl_rate + shock) / 12
    return requested_amount * r / (1 - (1 + r) ** -requested_term)


def pl_shock_50bp_fits(pl_shock_50bp_instalment: float, pl_affordable_instalment: float) -> bool:
    return pl_shock_50bp_instalment <= pl_affordable_instalment


def pl_shock_75bp_instalment(requested_amount: float, requested_term: float, pl_rate: float, shock: float = param(0.0075)) -> float:
    r = (pl_rate + shock) / 12
    return requested_amount * r / (1 - (1 + r) ** -requested_term)


def pl_shock_75bp_fits(pl_shock_75bp_instalment: float, pl_affordable_instalment: float) -> bool:
    return pl_shock_75bp_instalment <= pl_affordable_instalment


def pl_shock_100bp_instalment(requested_amount: float, requested_term: float, pl_rate: float, shock: float = param(0.01)) -> float:
    r = (pl_rate + shock) / 12
    return requested_amount * r / (1 - (1 + r) ** -requested_term)


def pl_shock_100bp_fits(pl_shock_100bp_instalment: float, pl_affordable_instalment: float) -> bool:
    return pl_shock_100bp_instalment <= pl_affordable_instalment


def pl_shock_125bp_instalment(requested_amount: float, requested_term: float, pl_rate: float, shock: float = param(0.0125)) -> float:
    r = (pl_rate + shock) / 12
    return requested_amount * r / (1 - (1 + r) ** -requested_term)


def pl_shock_125bp_fits(pl_shock_125bp_instalment: float, pl_affordable_instalment: float) -> bool:
    return pl_shock_125bp_instalment <= pl_affordable_instalment


def pl_shock_150bp_instalment(requested_amount: float, requested_term: float, pl_rate: float, shock: float = param(0.015)) -> float:
    r = (pl_rate + shock) / 12
    return requested_amount * r / (1 - (1 + r) ** -requested_term)


def pl_shock_150bp_fits(pl_shock_150bp_instalment: float, pl_affordable_instalment: float) -> bool:
    return pl_shock_150bp_instalment <= pl_affordable_instalment


def pl_shock_175bp_instalment(requested_amount: float, requested_term: float, pl_rate: float, shock: float = param(0.0175)) -> float:
    r = (pl_rate + shock) / 12
    return requested_amount * r / (1 - (1 + r) ** -requested_term)


def pl_shock_175bp_fits(pl_shock_175bp_instalment: float, pl_affordable_instalment: float) -> bool:
    return pl_shock_175bp_instalment <= pl_affordable_instalment


def pl_shock_200bp_instalment(requested_amount: float, requested_term: float, pl_rate: float, shock: float = param(0.02)) -> float:
    r = (pl_rate + shock) / 12
    return requested_amount * r / (1 - (1 + r) ** -requested_term)


def pl_shock_200bp_fits(pl_shock_200bp_instalment: float, pl_affordable_instalment: float) -> bool:
    return pl_shock_200bp_instalment <= pl_affordable_instalment


def pl_shock_225bp_instalment(requested_amount: float, requested_term: float, pl_rate: float, shock: float = param(0.0225)) -> float:
    r = (pl_rate + shock) / 12
    return requested_amount * r / (1 - (1 + r) ** -requested_term)


def pl_shock_225bp_fits(pl_shock_225bp_instalment: float, pl_affordable_instalment: float) -> bool:
    return pl_shock_225bp_instalment <= pl_affordable_instalment


def pl_shock_250bp_instalment(requested_amount: float, requested_term: float, pl_rate: float, shock: float = param(0.025)) -> float:
    r = (pl_rate + shock) / 12
    return requested_amount * r / (1 - (1 + r) ** -requested_term)


def pl_shock_250bp_fits(pl_shock_250bp_instalment: float, pl_affordable_instalment: float) -> bool:
    return pl_shock_250bp_instalment <= pl_affordable_instalment


def pl_shock_275bp_instalment(requested_amount: float, requested_term: float, pl_rate: float, shock: float = param(0.0275)) -> float:
    r = (pl_rate + shock) / 12
    return requested_amount * r / (1 - (1 + r) ** -requested_term)


def pl_shock_275bp_fits(pl_shock_275bp_instalment: float, pl_affordable_instalment: float) -> bool:
    return pl_shock_275bp_instalment <= pl_affordable_instalment


def pl_shock_300bp_instalment(requested_amount: float, requested_term: float, pl_rate: float, shock: float = param(0.03)) -> float:
    r = (pl_rate + shock) / 12
    return requested_amount * r / (1 - (1 + r) ** -requested_term)


def pl_shock_300bp_fits(pl_shock_300bp_instalment: float, pl_affordable_instalment: float) -> bool:
    return pl_shock_300bp_instalment <= pl_affordable_instalment


def pl_shock_325bp_instalment(requested_amount: float, requested_term: float, pl_rate: float, shock: float = param(0.0325)) -> float:
    r = (pl_rate + shock) / 12
    return requested_amount * r / (1 - (1 + r) ** -requested_term)


def pl_shock_325bp_fits(pl_shock_325bp_instalment: float, pl_affordable_instalment: float) -> bool:
    return pl_shock_325bp_instalment <= pl_affordable_instalment


def pl_shock_350bp_instalment(requested_amount: float, requested_term: float, pl_rate: float, shock: float = param(0.035)) -> float:
    r = (pl_rate + shock) / 12
    return requested_amount * r / (1 - (1 + r) ** -requested_term)


def pl_shock_350bp_fits(pl_shock_350bp_instalment: float, pl_affordable_instalment: float) -> bool:
    return pl_shock_350bp_instalment <= pl_affordable_instalment


def pl_shock_375bp_instalment(requested_amount: float, requested_term: float, pl_rate: float, shock: float = param(0.0375)) -> float:
    r = (pl_rate + shock) / 12
    return requested_amount * r / (1 - (1 + r) ** -requested_term)


def pl_shock_375bp_fits(pl_shock_375bp_instalment: float, pl_affordable_instalment: float) -> bool:
    return pl_shock_375bp_instalment <= pl_affordable_instalment


def pl_shock_400bp_instalment(requested_amount: float, requested_term: float, pl_rate: float, shock: float = param(0.04)) -> float:
    r = (pl_rate + shock) / 12
    return requested_amount * r / (1 - (1 + r) ** -requested_term)


def pl_shock_400bp_fits(pl_shock_400bp_instalment: float, pl_affordable_instalment: float) -> bool:
    return pl_shock_400bp_instalment <= pl_affordable_instalment


def pl_shock_425bp_instalment(requested_amount: float, requested_term: float, pl_rate: float, shock: float = param(0.0425)) -> float:
    r = (pl_rate + shock) / 12
    return requested_amount * r / (1 - (1 + r) ** -requested_term)


def pl_shock_425bp_fits(pl_shock_425bp_instalment: float, pl_affordable_instalment: float) -> bool:
    return pl_shock_425bp_instalment <= pl_affordable_instalment


def pl_shock_450bp_instalment(requested_amount: float, requested_term: float, pl_rate: float, shock: float = param(0.045)) -> float:
    r = (pl_rate + shock) / 12
    return requested_amount * r / (1 - (1 + r) ** -requested_term)


def pl_shock_450bp_fits(pl_shock_450bp_instalment: float, pl_affordable_instalment: float) -> bool:
    return pl_shock_450bp_instalment <= pl_affordable_instalment


def pl_shock_475bp_instalment(requested_amount: float, requested_term: float, pl_rate: float, shock: float = param(0.0475)) -> float:
    r = (pl_rate + shock) / 12
    return requested_amount * r / (1 - (1 + r) ** -requested_term)


def pl_shock_475bp_fits(pl_shock_475bp_instalment: float, pl_affordable_instalment: float) -> bool:
    return pl_shock_475bp_instalment <= pl_affordable_instalment


def pl_shock_500bp_instalment(requested_amount: float, requested_term: float, pl_rate: float, shock: float = param(0.05)) -> float:
    r = (pl_rate + shock) / 12
    return requested_amount * r / (1 - (1 + r) ** -requested_term)


def pl_shock_500bp_fits(pl_shock_500bp_instalment: float, pl_affordable_instalment: float) -> bool:
    return pl_shock_500bp_instalment <= pl_affordable_instalment


def pl_shock_525bp_instalment(requested_amount: float, requested_term: float, pl_rate: float, shock: float = param(0.0525)) -> float:
    r = (pl_rate + shock) / 12
    return requested_amount * r / (1 - (1 + r) ** -requested_term)


def pl_shock_525bp_fits(pl_shock_525bp_instalment: float, pl_affordable_instalment: float) -> bool:
    return pl_shock_525bp_instalment <= pl_affordable_instalment


def pl_shock_550bp_instalment(requested_amount: float, requested_term: float, pl_rate: float, shock: float = param(0.055)) -> float:
    r = (pl_rate + shock) / 12
    return requested_amount * r / (1 - (1 + r) ** -requested_term)


def pl_shock_550bp_fits(pl_shock_550bp_instalment: float, pl_affordable_instalment: float) -> bool:
    return pl_shock_550bp_instalment <= pl_affordable_instalment


def pl_shock_575bp_instalment(requested_amount: float, requested_term: float, pl_rate: float, shock: float = param(0.0575)) -> float:
    r = (pl_rate + shock) / 12
    return requested_amount * r / (1 - (1 + r) ** -requested_term)


def pl_shock_575bp_fits(pl_shock_575bp_instalment: float, pl_affordable_instalment: float) -> bool:
    return pl_shock_575bp_instalment <= pl_affordable_instalment


def pl_shock_600bp_instalment(requested_amount: float, requested_term: float, pl_rate: float, shock: float = param(0.06)) -> float:
    r = (pl_rate + shock) / 12
    return requested_amount * r / (1 - (1 + r) ** -requested_term)


def pl_shock_600bp_fits(pl_shock_600bp_instalment: float, pl_affordable_instalment: float) -> bool:
    return pl_shock_600bp_instalment <= pl_affordable_instalment


def pl_shock_625bp_instalment(requested_amount: float, requested_term: float, pl_rate: float, shock: float = param(0.0625)) -> float:
    r = (pl_rate + shock) / 12
    return requested_amount * r / (1 - (1 + r) ** -requested_term)


def pl_shock_625bp_fits(pl_shock_625bp_instalment: float, pl_affordable_instalment: float) -> bool:
    return pl_shock_625bp_instalment <= pl_affordable_instalment


def pl_shock_650bp_instalment(requested_amount: float, requested_term: float, pl_rate: float, shock: float = param(0.065)) -> float:
    r = (pl_rate + shock) / 12
    return requested_amount * r / (1 - (1 + r) ** -requested_term)


def pl_shock_650bp_fits(pl_shock_650bp_instalment: float, pl_affordable_instalment: float) -> bool:
    return pl_shock_650bp_instalment <= pl_affordable_instalment


def pl_shock_675bp_instalment(requested_amount: float, requested_term: float, pl_rate: float, shock: float = param(0.0675)) -> float:
    r = (pl_rate + shock) / 12
    return requested_amount * r / (1 - (1 + r) ** -requested_term)


def pl_shock_675bp_fits(pl_shock_675bp_instalment: float, pl_affordable_instalment: float) -> bool:
    return pl_shock_675bp_instalment <= pl_affordable_instalment


def pl_shock_700bp_instalment(requested_amount: float, requested_term: float, pl_rate: float, shock: float = param(0.07)) -> float:
    r = (pl_rate + shock) / 12
    return requested_amount * r / (1 - (1 + r) ** -requested_term)


def pl_shock_700bp_fits(pl_shock_700bp_instalment: float, pl_affordable_instalment: float) -> bool:
    return pl_shock_700bp_instalment <= pl_affordable_instalment


def pl_shock_725bp_instalment(requested_amount: float, requested_term: float, pl_rate: float, shock: float = param(0.0725)) -> float:
    r = (pl_rate + shock) / 12
    return requested_amount * r / (1 - (1 + r) ** -requested_term)


def pl_shock_725bp_fits(pl_shock_725bp_instalment: float, pl_affordable_instalment: float) -> bool:
    return pl_shock_725bp_instalment <= pl_affordable_instalment


def pl_shock_750bp_instalment(requested_amount: float, requested_term: float, pl_rate: float, shock: float = param(0.075)) -> float:
    r = (pl_rate + shock) / 12
    return requested_amount * r / (1 - (1 + r) ** -requested_term)


def pl_shock_750bp_fits(pl_shock_750bp_instalment: float, pl_affordable_instalment: float) -> bool:
    return pl_shock_750bp_instalment <= pl_affordable_instalment


def pl_shock_775bp_instalment(requested_amount: float, requested_term: float, pl_rate: float, shock: float = param(0.0775)) -> float:
    r = (pl_rate + shock) / 12
    return requested_amount * r / (1 - (1 + r) ** -requested_term)


def pl_shock_775bp_fits(pl_shock_775bp_instalment: float, pl_affordable_instalment: float) -> bool:
    return pl_shock_775bp_instalment <= pl_affordable_instalment


pl_stress = flow(
    pl_shock_25bp_instalment,
    pl_shock_25bp_fits,
    pl_shock_50bp_instalment,
    pl_shock_50bp_fits,
    pl_shock_75bp_instalment,
    pl_shock_75bp_fits,
    pl_shock_100bp_instalment,
    pl_shock_100bp_fits,
    pl_shock_125bp_instalment,
    pl_shock_125bp_fits,
    pl_shock_150bp_instalment,
    pl_shock_150bp_fits,
    pl_shock_175bp_instalment,
    pl_shock_175bp_fits,
    pl_shock_200bp_instalment,
    pl_shock_200bp_fits,
    pl_shock_225bp_instalment,
    pl_shock_225bp_fits,
    pl_shock_250bp_instalment,
    pl_shock_250bp_fits,
    pl_shock_275bp_instalment,
    pl_shock_275bp_fits,
    pl_shock_300bp_instalment,
    pl_shock_300bp_fits,
    pl_shock_325bp_instalment,
    pl_shock_325bp_fits,
    pl_shock_350bp_instalment,
    pl_shock_350bp_fits,
    pl_shock_375bp_instalment,
    pl_shock_375bp_fits,
    pl_shock_400bp_instalment,
    pl_shock_400bp_fits,
    pl_shock_425bp_instalment,
    pl_shock_425bp_fits,
    pl_shock_450bp_instalment,
    pl_shock_450bp_fits,
    pl_shock_475bp_instalment,
    pl_shock_475bp_fits,
    pl_shock_500bp_instalment,
    pl_shock_500bp_fits,
    pl_shock_525bp_instalment,
    pl_shock_525bp_fits,
    pl_shock_550bp_instalment,
    pl_shock_550bp_fits,
    pl_shock_575bp_instalment,
    pl_shock_575bp_fits,
    pl_shock_600bp_instalment,
    pl_shock_600bp_fits,
    pl_shock_625bp_instalment,
    pl_shock_625bp_fits,
    pl_shock_650bp_instalment,
    pl_shock_650bp_fits,
    pl_shock_675bp_instalment,
    pl_shock_675bp_fits,
    pl_shock_700bp_instalment,
    pl_shock_700bp_fits,
    pl_shock_725bp_instalment,
    pl_shock_725bp_fits,
    pl_shock_750bp_instalment,
    pl_shock_750bp_fits,
    pl_shock_775bp_instalment,
    pl_shock_775bp_fits,
    name="stress_tests",
)
