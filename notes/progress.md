# Progress log

One entry per merged task: date, task, model, commit, outcome, tests, follow-ups.

## 2026-09-23

- **T0.1** (opus) `1bd587b`: `CLAUDE.md` conventions plus `tests/test_conventions.py` (500-line limit with allowlist; bans `doc NN`, `§`, `EXPERIMENTS`). 2 passed. Follow-up: `CLAUDE.md` points at `Design.md` §8 and `IR.md` §9 for the layout; repoint when those docs are removed (T7.1). Worktree came off `main` and was fast-forwarded to `feature/decider-v2`; briefs now tell agents to check their base.
- **T0.2** (opus) `a4bc8aa`, merge `e950786`: `import decider, decider.serving` works; the handler's `module_fn` and `DeciderConfigSettings.get()` raise `NotImplementedError` until serving and the config store are rebuilt (T6.1/T6.2); `get_default_executor` removed; legacy tests moved to `tests/_legacy/` and ignored. 3 passed. Follow-up: `serving/servers/{starlette,sanic}.py` still import `decider.initialization` at server startup (T6.2).
- **T0.4** (opus) `b046335`: 10 decision notes plus `notes/README.md` index. Findings that need a decision or care later: fusion default (Design says one kernel per sequence, evidence says fusing many heavy steps loses; decide before T3.2/T3.3); the fallback also has to catch `UnsupportedBytecodeError` (not a `NumbaError`); the compile content key must cover constants, not just `co_code` (T3.2); decider2 shipped `nogil` opt-in, the new rule is unconditional for serving.

**Phase 0 complete.** `uv run pytest`: 3 passed. Conventions test green.
- **T2.1** (opus) `a44e63a`: registry rewrite. Import-path tags plus `Literal` aliases, import-path loading only returns registered subclasses of the root, duplicate-alias error names both, same-qualname replacement, did-you-mean, `json_schema()` per class, `ref(Root)` = `Annotated[Root, WrapValidator(dispatch), SerializeAsAny()]`. 23 registry tests; suite 26 passed. Notes: a subclass inheriting a parent's alias must redeclare `type`; `ref` field JSON schema is the root's, not a oneOf (UI uses `json_schema()`).
- **T1.2** (opus) `73302ef`: `engine/ir/decls.py` (NullPolicy, FeatureKind, Input, Output, ParamDecl) and `engine/params/` (declare, harvest, models, bundles, validate). `param()` accepts any default incl. bool/None, `param(required=True)`; `call_with_defaults` for `FunctionStep.__call__`; per-node pydantic model; `ParamsCache` keyed by (document SHA-256, path); `on_invalid` applied at validation. Suite 64 passed. Follow-ups: unknown top-level namespace check (T1.4); `parameters()`/`.defaults()` (T1.1); `bind()` tests (T1.1); `str` params encoded through column categories (boundary/compile, T3.x); bare Python defaults still rejected (as decider2); ponytail: cache unbounded, non-JSON docs keyed by content only.
- **orchestrator** `8b3c953`: `decider/steps/values.py` (ParamRef, Value[T], TableRef, TableValue) written up front so T1.1 and T4.1 share it.
- **T4.1** (opus) `2143b7c`: `steps/expr/` (parse, `Expr.evaluate` AST interpreter, no eval/emit) and `steps/trees/schema/` (conditions, nodes, normalised `Tree`, `V3TreeDocument`, flat-rule and prioritised flat-rule documents, `TreeDocument` union with version detection; v0/v1/v2/unknown raise a deprecation error). Flat and v3 normalise to one `Tree(name, mode, rules, nodes, output)`. Suite 236 passed. Deferred to T4.2: every test that runs a tree (list in the T4.1 report: test_trees*, test_typed_features, test_trees_strings, expr end-to-end). Choices: thresholds `Value[int | float]`; unversioned doc = v3; `{"key": k}` read as ParamRef; empty composites rejected.
- **T3.1** (opus) `9d97a77`: `engine/boundary/` (dtypes, nulls, extract, writeback) and `_arrow/` (plan, view, kernels, intrinsics, shim loader). The fixed C shim (decider2's C + vendored nanoarrow) is compiled with `cc` on first use into a content-keyed cache and loaded with ctypes; no Python headers needed. REQUIRED nulls raise `MissingInputError(input, path, null_count)`; routing, NOT_APPLICABLE_AS and fill reporting removed. 175 boundary tests, 0 skipped. Deferred: apply/score/equivalence integration tests (T1.4/T3.3), `numpy_dtype` half of the shim test (T3.2). Runner must import `extract` lazily (needs a compiler on first use). ponytail: POSIX cc only; multi-chunk streams refused; unbounded plan cache.
- **T1.1** (opus) `1e120ff`: `steps/` (base, function, frame, sequential, dag, branch, loop, configurable) and `engine/ir/` (origin, nodes, context with `to_ir` checks and relabel), `engine/params/schema.py` (`parameters()`, `ParamsSchema`). `decider/modules/` deleted. All IR acceptance tests 1–10 pass. Suite after merging T3.1 + T1.1: 498 passed. Key facts for later tasks: a scalar CallNode's `Input.name` is the resolved column (relabel), so runners pass inputs positionally; anonymous flows/dags inline; unnamed branch arms are `arm{i}`, loop body `body`; `to_ir` cache keyed by `id(step)` with weakref finalize; `bind()` changes the default only; `f | g` is Python's own TypeError. ponytail: loop carry relabel mismatch unchecked; `json_schema()` dangling `$defs`.
- **T6.1** (opus) `a5fdda3`: `decider/config/` (Version, VersionedConfig, `ConfigStore` registry root, `JsonFileStore` with atomic staged writes). Synchronous; no polling; errors propagate. `pull_version` bug fixed (compared `Version` against `target.version`, an AttributeError hidden by the polling loop's `except`), with regression tests. `settings.config.get()` builds the store; handler `init_fn` just calls `get_latest()`. Dropped: YAML/TOML backends, structure-in-config helpers, legacy GraphModule round-trip tests. Suite 513 passed. ponytail: no lock for concurrent pulls.
- **Review of T1.1/T3.1/T4.1** (opus, code-review skill): 6 must-fix (argument names lost under relabel; literal `Value` has no IR slot; `ParamDecl` unhashable; unvalidated step names; `ConfigurableStep` helper pattern breaks; no path → Step map) and 11 should-fix. Dispatched as **T1.1b**. Deferred to a cleanup after T3.3: dead `trees/ops.py` (T4.2 may use it), unused shim C exports, ~150 lines of boundary API only tests use, boundary duplication (`explain_boundary`, dtype alias tuple, frame-level `categories`), `sequential.py` double merge.
- **T1.3** (opus) `d425800`: `engine/wiring/` (`interface`, `resolve -> Plan` with nested `Call/Sequence/Branch/Loop`, `Merge`, `Carry`, flat `calls` for the compiler, `versions`, `chains`, `inputs`, `outputs`, `drops`). Ids are positions. Barriers: frame steps with unknown outputs; runner checks their columns at run time. Branch arms and loop bodies are scopes (only `modifies`/`carries` leave). Suite 557 passed. Left for T1.4: passthrough of unread frame columns, shadowing check, all execution tests (flagship, spec_conformance, review_findings 2-4). Not ported: decider2 loop limits (body must read carry, `loop_idx`, single carry).
- **T1.1b** (opus) `35daa05`: review fixes. `Input.arg` (argument name; `name` is the resolved column); `CallNode.consts`; scalar call `fn(**{i.arg: v}, **consts, **params)`, row call `fn(row, params, consts)`, reference `reference(row, params, consts, visit)`; `ParamDecl` hashable (schema as pairs); `check_name` rejects "", "/" and "#"; `IRContext.expand(owner, helper)`; `engine.step_map(step)` covers arms/conditions/bodies. Tree-schema dedup (CompositeNode alias, `_Bounds` mixin, `BaseTreeDocument`, `get_field`). Suite after merge with T1.3: 581 passed.
- **T3.2** (opus) `2aca9ca`: `engine/compile/` (fingerprint incl. co_consts/closures/globals, content-shared dispatchers, intrinsic fused kernel `kernel(n, cols, valids, params, outs)` with nogil, `compile_plan(plan, fuse=True|False) -> {call_id: Kernel|Fallback}`). No codegen. Benchmark vs decider2 (20 steps, 1M rows): batch ratio ~1.00, n=1 call 0.73× (faster); outputs bit-identical. Suite 629 passed. Follow-ups: `bytes` span inputs not supported in kernels (needed by string trees, T4.2); CPU-target check belongs to build/serve (T6.3); kernels write no validity masks; stepped = one kernel per call.
- **T1.4** (opus) `7ac8d21`: `engine/run/` (`State` keyed by Version.id with `record()` override hook and `name@path`/`name@*` lookup; `Checkpoint(origin, when="before"|"after")`; `InterpretedRunner` for scalar/row/frame/branch/loop; `Engine(params_validation)`, `bind(step, mode) -> Executable` with `prepare/run/score`, `RUNNERS` registry; `RunParams`/`RunReport`; namespace checks at every depth). `Step.run` wired. §7 example runs in interpreted mode. Suite after merging with T3.2: 718 passed. Notes: decider2's compiled path can't run here (its `_nashim` isn't built for CPython 3.14), so ported tests pin decider2's own expected values; interpreted mode passes `str` columns as Python str (compiled modes use codes — T3.3 must reconcile); frame steps can't change row count; two checkpoints per node.
- **T2.2** (opus) `40e00d0`: `ConfigurableStep.load`, `ParamDecl.arg` (ratified in spec-amendments), `IRContext.value(..., arg=)`; tests with an example `ThresholdRule` and nested `RuleSet` (StepRef list + `ctx.expand`): JSON round-trip gives identical output, retune without IR rebuild, config store round-trip. Suite 728 passed. Usability friction for T2.3: IR types not exported from `decider`/`decider.engine`; params/consts split per config is manual (`ctx.values` helper suggested).
- **T1.5** (opus) `5a88805`: `engine/debug/` (`Session` over any `Runner`; `break_at` path/prefix/predicate, `risk_tree#n17` via row-node visits reported at the node's after-checkpoint; `step`/`step_into`/`resume`/`pause`/`set`/`rewind`; events and commands as frozen dataclasses; JSON via `EVENT`, `EVENT_LOG`, `COMMAND` TypeAdapters; summaries = dtype, n, nulls, 5 values). `Step.session` and `Executable.session` wired. Suite 756 passed. ponytail: `set` overwrites earlier versions in place; frame steps don't see overridden inputs; rewind replays branch routing from original values. Follow-up: session reaches into `State._extra` / `_base` (private).

**Phase 1 complete.** IR acceptance tests pass; §7 example runs interpreted; sessions work; events round-trip as JSON.
- **T2.3** (opus) `6a6474e`: `notes/usability-review-1.md` from docstrings alone; all three pipelines ran. Top findings: IR classes not exported so custom configs can't be written from the public API (blocker); emit/drop undiscoverable and emit paths relative while everything else is absolute; arm writes outside `modifies` silently lost; no common error base, no step context on user errors; front-door gaps. Triaged into **T2.3b** (all but step-context on user errors, which waits for T3.3 because it touches runners).
- **T6.4** (opus) `9fbc373`: `decider/serving/session_ws.py` — `session_app(factory) -> Starlette`, one session per connection; commands queued to a worker thread, `pause` applied immediately, explicit `value` requests return full columns, malformed messages get `rejected` and the socket stays open. starlette added to the dev dependency group (already an extra). 4 tests with a raw ASGI client (no httpx).
- **T4.4** (opus) `c06c258`: `steps/scorecard/` (`ScorecardConfig`, alias `scorecard`, decider_old document shape; one row CallNode per scored variable with edges/points as consts and ParamRefs slotted in at run time; adjusted/constant as scalar nodes; total as a row node). All 6 decider_old cases pass; njit-compatible (string values bins fall back to Python until kernels take dictionary codes). Kept decider_old's "bound of 0 counts as unset" quirk for identical scores. Not ported: ProbabilityDefault/LogProbability/ScoreFromPDO/MergeScorecardValues (untested in decider_old). Suite 783 passed.
- **orchestrator fix**: `State.from_frame` passed an object column's validity mask as `rows` (crash on nullable `str` inputs); fixed with a regression test.
- **T3.3 part A** (opus) `d4099ee`: `SteppedRunner` (subclasses interpreted, compiled units per call) and `FusedRunner` (one checkpoint pair per kernel with its first step's origin); `score()` fast path (one array per dtype, no polars) in every mode without frame steps; string params/inputs as int32 codes from a per-runner table (bundles cacheable); nullable outputs carry masks, kernels split after a nullable value; lazy validation in fused = validate a kernel's nodes just before launch (see `notes/lazy-validation-fused.md`). Benchmarks (flagship, 1M rows; 20k scores): decider fused 12.5M rows/s vs decider2 8.2M (1.5×); score p50 27.7 µs vs 26.5 µs, reruns +2–5%; p99 34.7 vs 40.6 µs; within decider2's 60 µs spec. Merge fixes: njit probe uses `d.arg` + int32 for str params; session uses `base_annotation`. Open: fused-kernel NumbaError at kernel level propagates instead of falling back; `str` outputs, `bytes` inputs, `list[str]` params unsupported in kernels; breakpoints inside a fused kernel need mapping (part B).
- **T2.3b** (opus) `8c0116f`: `ctx.call` helper and IR exports; absolute `name@path`; reading a name whose only writer is inside a branch/loop scope is a `WiringError`; `DeciderError` base with `WiringError`, `IRError`, and moved `ParamsError`/`MissingInputError`/arrow errors; package quickstart docstring; `Engine`, `Value`, `ParamRef` top-level; lambda steps named by `name=`; `load` accepts JSON text. Left: `bind(mode=)` Literal (engine.py), registry/expr/engine ValueErrors not yet `DeciderError`. Suite after both merges: 1023 passed.
- **T6.2** (opus) `4aa7fc7`: serving. `RequestHandler(store, pipeline="module:attr", mode="fused")` with `stage`/`activate`/`rollback` (build + warm off the request path; atomic reference swap; lock only for swaps). Pipeline is a `Step` or a function whose argument names are config-version keys loaded via `ConfigurableStep.load`; `"params"` key is the params document. JSON object → `score(dict)`, arrays/CSV/Parquet → `run(df)`. `/ping` and `/invocations` 503 until active. `create_app(handler=None)` for starlette/sanic; stale `decider.initialization` removed; settings trimmed. 20 serving tests. Dropped: decider2 params-play routes, sealed/live modes. Open: warm-up uses made-up values (no user sample); cross-process swaps need an admin route or restart; sanic untested (not installed).
- **T3.4** (opus) `7f9667e`: `decider/testing/` (`assert_equivalent` — exact run() equality across modes, score()==run() per row, session-to-end == run, optional scripted session; `corpus`; `no_recompile` via numba's compile event). Worked example runs through `assert_equivalent` with a scripted break/set. Found: interpreted mode passes numpy scalars, so x/0 gives inf/nan while compiled modes raise ZeroDivisionError (strict xfail, sent to T3.3B); fused breakpoint inside a kernel (strict xfail, T3.3B).
- **T3.3 part B** (opus) `fcfd72b`: sessions in stepped/fused (fused breakpoints and rewinds inside a kernel pause at the kernel start; `Paused.kernel` lists its steps; values never stored by a kernel raise a KeyError suggesting stepped mode); `add_note("in step <path>")` on user errors in every mode (row index in interpreted); `Kernel.run` falls back per call on launch-time `NumbaError`/`UnsupportedBytecodeError`; `Mode` Literal; `EngineError`, `RegistryError`, `ExprError` on `DeciderError`; `State.restore`. Suite 1109 passed, 2 xfailed (x/0 divergence → T5.1; fused in-kernel break is by design, xfail reason updated).
- **T6.3** (opus) `8eaad7d`: `decider/cli/` — `build [VERSION]` (stage: import, load, bind, validate, warm → numba disk cache; records `cpu_target()` in `decider-cpu-target.json`), `serve` (uvicorn factory or sanic; warns on CPU-target mismatch), `template NAME [DIR]` (static starter project with one `{{name}}` substitution). CLI flags pass to workers via `DECIDER_*` env vars. Dropped: decider2 pipeline discovery (settings `module:attr` instead), `build --verify`. 12 tests.
- **T6.5** (opus) `ed46c44`: `session.replace(path, step)` / `session.delete(path)` (`decider/engine/debug/edit.py`): the new step takes the old name/path; IR rebuilt (cache reuses unchanged subtrees), plan re-resolved, values carried over by (name, producer), overrides re-recorded; runners gained a `skip` hook so upstream nodes aren't re-run; pauses before the edited node with reason "edit". `Edited` event, `Delete` (JSON) and `Replace` (Python-only) commands; websocket drives delete. Fixed stale `_converted` in `SteppedRunner._compile`. 29 tests, all modes. ponytail: edits inside a loop re-run it from iteration 1.
- **T5.1 + T5.2** (opus) `eab3fbf`: branch/loop stepping in every mode (`Checkpoint`/`NodeStarted`/`Paused` carry `arm` and `iteration`); fused packing of straight scalar branches/loops into one intrinsic kernel (`Fork`/`Repeat` LLVM blocks, no codegen; rules in `notes/packed-control-flow.md`), with per-launch fallback to the driven path; multi-step arms; several carries; interpreted mode passes Python scalars (x/0 now agrees across modes). Wiring: arms must agree on modified types; loop carry version ordered after body writes. Benchmarks vs decider2 fused: branch 1M rows 42–44M vs 3.3–3.5M rows/s; loop 6.3M vs 0.64M rows/s. Merge with T6.5 resolved in `session._index` and `SteppedRunner._compile`. Suite 1203 passed, 1 xfailed (by design).
- **ponytail audit** (opus, read-only): biggest finding — the Arrow boundary (`engine/boundary/_arrow`, C shim, vendored nanoarrow) is slower than `Series.to_numpy()` (fused run 1M rows: 97.6 ms vs 3.1 ms; 100k: 6.07 vs 0.36 ms) and production only uses its float/int/bool slice. **Decision:** replace with polars casting + `to_numpy` (~15 lines), delete `_arrow/`, `extract.py`, `writeback.py`, nanoarrow and the runtime C compiler (after T4.2 confirms trees don't need raw string spans). Also accepted: `serializable/` leftovers, dead `exceptions.py` code, `ConfigStore` loaded-version state, `sequential.py` double merge, one did-you-mean helper, small duplicates and test-only helpers, serving odds and ends, examples on user-facing public docstrings. Kept: sanic backend (deployment choice), `nogil` flag (IR.md pins it). Runs as the cleanup task after T4.2.
- **Reversed (user, 2026-09-23):** the Arrow shim stays. It exists so strings (patterns, regexes) are matched raw at the node without preprocessing into codes. T4.2 told to use STR spans through the shim and add span support to kernels/row units. The cleanup task will instead find and fix why the boundary path is ~30× slower than `to_numpy` for numeric columns, and keep the shim API that string handling needs.
- **Priority (user):** about 90% of use is single-record. Every performance change is judged first on `score(dict)` p50/p99 and one-row `run`, then batch throughput. The boundary fix must not regress single-record latency.
- **T4.2** (opus) `2779801`, merge `cf6560e` (conflicts resolved by a merge agent: `Layout.source` takes the input so bytes stay unmasked; `Kernel` keeps both launch fallback and Literal decode; packed kernels never emit Literals): `TreeConfig` (alias `tree`) → one row CallNode; generic njit walker over address-passed int/float/byte arrays; Python reference walker with `visit`; expressions as postfix data; string matching on raw bytes via the shim; flat, v3 and prioritised documents. 286 tree tests, all modes, reference == compiled on every fixture. Bench vs decider2 (127-node tree, loaded box): numeric 592 vs 748 ns/row, score p50 58 vs 209 µs; string-gated 689 vs 614 ns/row, score p50 101 vs 257 µs. Suite 1413 passed, 1 xfailed. Follow-ups: numeric nulls raise instead of decider_old's route-to-otherwise (honour the document's `null_handling`); string batch path copies the column before shim export; decision tables deferred to T4.3.
- **C1** (opus) `9f4ed5b`: Arrow boundary made fast with the shim kept — new `sm_columns` reads whole columns in one C call (memcpy when clean), large clean numeric columns are zero-copy views owned by the moved `ArrowArray`, per-call Python overhead removed, string spans export the frame column in place. Fused, flagship: 1-row run 851 µs (decider2) → 122 µs; 100k 12.1 → 0.73 ms; 1M 231 → 5.7 ms; score p50 29 µs (unchanged path). Tree 1M 1216 → 227 ms; string-gated 1018 → 360 ms. Cleanup: serializable leftovers, dead exceptions, ConfigStore loaded state, `flow()` double flattening, one did-you-mean helper, duplicates, unused shim exports, `writeback.py`, serving enum/formatters, examples on user-facing docstrings, boundary rationale moved to notes. +738/−1278 lines. Suite after merge 1400 passed, 1 xfailed. ponytail: string columns still become Python objects in `from_frame` even when only kernels read them.
- **Old-test coverage** (opus) `a9877d2`, `notes/old-test-coverage.md`: 639 old tests → 495 ported, 85 dropped, 35 pending T4.3, 24 gaps. Triage: **port** the 10 real gaps (assert_equivalent when every mode crashes; `bind()` through compiled modes and validated; params-only stage/activate compiles nothing; first string-tree request compiles nothing after warm-up; editing string patterns never recompiles; tree walker disk-cache hit in a fresh process; 400-feature tree; >6 computed features; empty prioritised rules → default) and **add** an opt-in tree output naming the leaf that answered (decider_old path tracking; audit). **Drop** (recorded here): `Tree.default_tree()` (UI convenience), `contract=` snapshots, docstring-as-description/`Implements:`, lints 6a/6b/6c, decider2 ScorePlan internals (fields, row pool), the hostile frozen-field swap (`object.__setattr__` on a frozen step is out of contract), `assert_equivalent(mode=)` rejection, `p.bonus` in expressions (not in the grammar). **T7.1** deletes all of `decider2/` (its `evaluation/` and `example_projects/` import it), `decider_old/`, `tests/_legacy/`; benchmark scripts keep their decider half, decider2 numbers are recorded in these notes.
- **T4.3** (opus) `1b5f6b1`: `steps/tables/` (`DecisionTableConfig`: jump-program matcher over address-passed row arrays, Python reference matcher, rows from params validated on arrival, raw-span string columns, decider_old document shape) and tree `null_handling` (default `otherwise`, as decider_old). 58 table tests, 14 tree null tests. Bench vs decider2: 12-band table score p50 27.4 vs 42.9 µs, batch 164 vs 304 ns/row; tree score p50 56 vs ~200 µs. Merge fix: table legacy loader used a schema helper removed by C1 (replaced by a 5-line dtype namer). Suite 1472 passed, 1 xfailed. Found: `document_key` JSON-encodes the whole params document on every `score()` (~40 µs with table rows) — fix in G1.
- **Spike: hot reload** (opus) branch `worktree-agent-a78dcc20376c11e46` `af24c37`, not merged; `notes/hot-reload-investigation.md` on that branch. One primitive `session.reload(new_pipeline)` (content diff in run order, re-run from the first change, `Edited` per path; `replace`/`delete` share it); notebook `session.watch(lambda: flow(...))` via IPython `post_run_cell`; files `ModuleWatcher("module:attr")` (mtime poll, re-import every user module under the folder, rollback on import error). Edit→output: interpreted ~5 ms; compiled ~150–190 ms for a body edit (only that kernel compiles), ~30 ms for a param-default edit. Serving never hot-reloads. Recommended follow-ups: `ReloadFailed` event, websocket reload via its command queue, a `structure` request for the UI, fingerprint DataFrames/arrays by value. Orchestrator recommendation: adopt after G1 (awaiting user).
- **G1** (opus) `6a4d023`: all 10 real coverage gaps ported (plus a real bug: serving warm-up fed `1.0` to `bytes` inputs, crashing any string-tree stage); `TreeConfig.path_output` (leaf id as a String output, both walkers, survives emit); `ParamsCache.key` hashes a reused document object once — `score()` with table rows from params 89 → 36 µs p50, flagship with a params doc 42 → 30 µs. `notes/old-test-coverage.md`: 0 GAP.
- **Hot reload merged** (user request) `af24c37`, merge `3718ff9`. Suite 1502 passed, 1 xfailed. Follow-ups dispatched as HR2.

## 2026-09-24

- **HR2** (opus) `158c5e4`: `ReloadFailed` event; `session_app(factory, watch="module:attr")` polls a `ModuleWatcher` and applies reloads through the command queue; `Session.structure()` and a websocket `structure` request; `fingerprint` hashes captured numpy arrays and DataFrames by value (a re-imported unchanged data constant is a no-op reload; ~0.5 ms per small frame, ~13 ms per 1M rows); usage examples on `reload`/`watch`/`ModuleWatcher`/`session_app`. Suite 1508 passed, 1 xfailed.
- **Spike: zero-copy Arrow** (opus) branch `worktree-agent-a7a1162a3b07a6e78` `702ee5e`, not merged; `notes/zero-copy-arrow-investigation.md` on that branch. In-kernel bitmap validity + in-place nullable/bool columns: wide null-heavy frame 1M rows ~2500 → ~370 ms, 100k 83–108 → 24.5 ms; flagship and tree unchanged (their inputs are already zero-copy); one kernel signature per unit. `score(dict)` through polars costs ~4× (flagship 31 → 126 µs, tree 60 → 231, wide 295 → 995). Spike recommends: adopt in-place reads for nullable/bool at ≥4096 rows, kernel-side bitmaps everywhere, `Enum.gather` for Literal outputs (−40 ms/1M tree rows) and `Series.set` for nullable outputs; keep the direct score() loader. Awaiting the user's call on score().
- **T7.1** (opus) `1c682c5`, merge `6008ac4`: deleted `decider2/`, `decider_old/`, `tests/_legacy/`, `experimentation/`, `Plan.md`, `Prompt.md`, `HANDOVER-stage2.md`, `Tasks.md`, `TYPED_FEATURES.md` and the decider2 wheels workflow (~1100 files, ~292k lines). Coverage map final: 639 old tests → 543 ported, 96 dropped, 0 gaps. Benchmarks renamed to decider-only (`modes`, `control_flow`, `tree_walk`, `table_match`); last decider2 numbers in `notes/benchmarks-vs-decider2.md`. README rewritten; CLAUDE.md repointed; `uv build` wheel contains only `decider/`. Design.md/IR.md references to the deleted migration docs repointed to `notes/`. Suite after merge with HR2: 1508 passed, 1 xfailed. Left: `docs/` Sphinx site and `projects/loan_scoring` describe decider 0.3; unused `visualise`/`eval` extras; stale `benchmarks/results*.csv`; untracked leftovers (`decider2/`, `experimentation/` logs and builds, `.decider2_cache/`, ~1.65 GB) left on disk for the user.

**Plan complete (T0.1–T7.1).**
