[build-system]
requires = ["maturin>=1.5,<2.0"]
build-backend = "maturin"

[project]
name = "decider-trees"
version = "0.1.0"
requires-python = ">=3.10"
dependencies = ["polars>=1.0"]

[tool.maturin]
features = ["pyo3/extension-module"]
module-name = "decider_trees._internal"
python-source = "."
