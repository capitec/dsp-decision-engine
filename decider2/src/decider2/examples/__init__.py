"""Small, self-contained pipelines for `decider2 serve` and for the serving
test suite (`tests/test_serving.py`, `tests/test_runtime_serve.py`) to
import — deliberately not `example_projects/`, which is a separate, much
larger corpus used for other purposes."""
